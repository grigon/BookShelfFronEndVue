import PasswordHelper from "../components/PasswordHelper";
import {helpers} from "@vuelidate/validators";

export const passwordValidator = {
    /* () => helpers.regex("capital letter", /[A-Z]/),
    containsLowercase:() => helpers.regex("lowercase",/[a-z]/),
    containsNumber: () => helpers.regex("number",/[0-9]/),containsUppercase:
    containsSpecial: helpers.regex("special character",/[#?!@$%^&*-]/),*/
};
